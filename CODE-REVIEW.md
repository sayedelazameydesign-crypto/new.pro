# 🔍 نقد بناء لمستودع نواة AI — الجولة الأولى

> **المنهج:** مراجعة هندسية صريحة: ما هو قوي (نُحافظ عليه)، وما يحتاج إصلاحًا (بترتيب أولوية)، مع حلول محددة قابلة للتنفيذ — **لا هدم، لا إعادة كتابة من الصفر.**
> **التاريخ:** 2026-08-29 · **المراجعة:** على الكود الموجود في `main` (بعد إضافة Groq + النشر الحي).

---

## 📊 التقييم العام

| البعد | التقييم | ملاحظة مختصرة |
|---|---|---|
| المعمارية وقابلية التوسع | **9/10** | الطبقات معزولة فعلاً — أثبتناها بإضافة مزود Groq في جلسة واحدة دون لمس الواجهة |
| الواجهة والتجربة | **8/10** | بث حي، RTL نظيف، إدارة محادثات كاملة |
| جودة الكود والاختبارات | **7/10** | 11/11 اختبارًا، لكن CI لا يشغّلها |
| النشر والتشغيل | **8/10** | CI + نشر تلقائي يعملان فعلاً (تحققنا) |
| **الأمان والحماية** | **4/10** | ⚠️ أخطر نقطة — الموقع أصبح عامًا والـ API بلا أي حماية |
| اكتمال الميزات المعلنة | **6/10** | ⚠️ إعداد "الإبداعية" ميت في الواجهة (لا يصل للخادم) |
| التوثيق | **9/10** | README + ARCHITECTURE + ROADMAP بالعربية، نافعة |

**الخلاصة:** نواة ممتازة كـ MVP (7.5/10)، جاهزة كمشروع حقيقي بعد إغلاق **3 ثغرات عالية الأولوية** (أسبوع واحد).

---

## ✅ نقاط القوة — حافظ عليها ولا تلمسها

1. **الطبقات معزولة حقًا** — المزود ملف واحد + سطران في المنسّق + سطر في السجل. Groq أضيف في 20 دقيقة والاختبارات 11/11 خضراء.
2. **قرار "لا يفشل أبدًا"** — التراجع التلقائي `Groq → Gemini → HF → Demo` يمنع تجربة ميتة. ذكي للإطلاق.
3. **المفاتيح لا تصل للعميل أبدًا** — تُقرأ من الـ env في الخادم فقط، و`/api/status` يكشف وجودها لا قيمتها.
4. **مقاومة XSS افتراضية** — `react-markdown` بلا raw HTML + هروب React.
5. **اختبارات بلا اعتماديات** — `node:test` مدمجة؛ البساطة ميزة، لا تستبدلها بإطار ثقيل الآن.
6. **النشر التلقائي يعمل** — GitHub CI يفحص، ثم Vercel ينشر كل push؛ وهذا نادر في مشاريع البداية.
7. **توثيق عربي حقيقي** — ARCHITECTURE.md يشرح "كيف تضيف مزودًا" بخطوات فعلية.

---

## 🔴 عالية الأولوية — أصلحها قبل أي ميزة جديدة

### F-01 · إعداد "الإبداعية (Temperature)" لا يعمل إطلاقًا ⚠️
- **أين:** `app/page.tsx` (يرسل `{messages, modelId, system}` فقط) + `app/api/chat/route.ts` (لا يقرأ temperature).
- **الخطأ:** المستخدم يحرّك المؤشر في الإعدادات ولا يحدث أي شيء — المزودات تستقبل القيمة الافتراضية 0.7 دائمًا.
- **الحل (10 دقائق):** تمرير `temperature` في الـ body → قراءته في الـ route → تمريره لـ `streamReply` ← كل مزود. سطران في كل ملف.
- **الأثر:** يجعل واجهة الإعدادات صادقة، ويحسّن جودة ردود Gemini/Groq للمستخدمين المتقدمين.

### F-02 · لا حماية إطلاقًا على `/api/chat` — والموقع الآن عام ⚠️🔥
- **أين:** `app/api/chat/route.ts`.
- **الخطأ:** أي شخص على الإنترنت يستطيع استدعاء الـ API مباشرة (curl) ويحرق حصة مفاتيحك المجانية خلال ساعات — بلا limit، بلا مصادقة، بلا حد لحجم الدخل.
- **الحل (نصف يوم):**
  1. **Rate limit** بسيط: Upstash Redis مجاني (20 طلب/دقيقة/IP) أو حارس في الذاكرة لكل instance (أفضل من لا شيء على Hobby).
  2. **سقف صارم:** أقصى رسائل سياق (موجود: 12) + أقصى `maxTokens` + رفض payloads أكبر من ~50KB.
  3. **متغير `APP_SECRET`** اختياري: يمنع الاتصال المباشر إذا أردت الإغلاق.
- **الأثر:** حماية حصتك المجانية من الاستنزاف المجهول — **أهم بند في هذه المراجعة**.

### F-03 · زر "إيقاف" لا يوقف المزود فعليًا
- **أين:** `app/page.tsx` (يُلغي fetch فقط) + `app/api/chat/route.ts` (يتجاهل انقطاع العميل).
- **الخطأ:** على Vercel، الدالة تستمر بالعمل حتى نهاية التوليد أو timeout الـ 60 ثانية — **استهلاك وقت/حصة بلا فائدة**.
- **الحل (30 دقيقة):** تمرير `req.signal` لـ `ReadableStream.start`؛ عند `abort` → `break` الحلقة فورًا. (Next.js يوفر ذلك تلقائيًا مع `request.signal`).
- **الأثر:** توفير حصة حقيقية، ورد فوري لزر الإيقاف.

### F-04 · التراجع الصامت لوضع العرض يضلل المستخدم
- **أين:** `lib/ai/index.ts` (fallback) + `app/page.tsx` (يعرض الموديل الاسم فقط).
- **الخطأ:** على الموقع الحي الآن، من يختار "Gemini 2.5 Flash" سيرد عليه **نص تجريبي محلي** ويظن أنه يتحدث مع Gemini.
- **الحل (30 دقيقة):** الخادم يرسل حدث `{provider}` فعلًا — استخدمه: اعرض شارة "تجريبي". وعند `provider === "demo"` اعرض بانر صغير: «الرد تجريبي — أضف مفتاحًا مجانيًا» (رابط الإعدادات).
- **الأثر:** صدق التجربة + تحويل الزوار إلى إضافة المفتاح (معدل تحويل أفضل).

---

## 🟠 متوسطة — اربطها في الجولة الثانية

### F-05 · CI لا يشغّل الاختبارات (يبني فقط)
- **أين:** `.github/workflows/ci.yml`.
- **الخطأ:** يعمل `install → typecheck → build` فقط؛ اختباراتنا الـ 11 لا تنفذ في CI، فأي كسر في `/api/chat` مستقبلاً يمرّ دون رصد.
- **الحل (30 دقيقة):** Job ثانٍ: `npm run build && npm run start &` ثم `npm test` (مع `BASE_URL`).
- **الأثر:** حماية الانحدارات — قلب أي نقد بنّاء.

### F-06 · Groq غير ممثل في الحالة والواجهة
- **أين:** `app/api/status/route.ts` + `lib/types.ts` (`ProviderStatus` لا يضم `groq`) + `SettingsModal`.
- **الخطأ:** أضفنا المزود الرابع، لكن لوحة الإعدادات تعرض Gemini/HF فقط — تشخيص خاطئ للطرفين.
- **الحل (20 دقيقة):** إضافة `groq` إلى `ProviderStatus` والـ route والواجهة.

### F-07 · ثنائية اللغة غير مكتملة
- **أين:** `app/page.tsx` ("ملف غير صالح"، "جاري الكتابة…"، `title="القائمة"`...) وعدة مواضع في `ModelPicker`.
- **الخطأ:** الادعاء "عربي/إنجليزي" بينما عشرات النصوص عربية ثابتة حتى في الوضع الإنجليزي.
- **الحل (ساعتان):** نقلها كلها إلى `lib/i18n.ts` — لا هدم، استبدال نص بنص.

### F-08 · مزود Hugging Face غير مُتحقق من الواقع
- **أين:** `lib/ai/providers/huggingface.ts` + موديلات `meta-llama/...` في `lib/models.ts`.
- **الخطأ:** بعض الموديلات gated/غير متاحة على الـ inference المجاني، وصياغة `router.huggingface.co` (`provider.meta`) **غير مؤكدة** — قد يفشل الطلب برسالة 404 مضللة.
- **الحل:** اختبار حقيقي بتوكن؛ جعل الـ router المحاولة الأولى؛ استبدال أي موديل لا يعمل بموديل مفتوح معروف (مثل `Qwen/Qwen2.5-7B-Instruct` المفتوح). اختبره مرة واحدة بتوكن وألغِ ما لا يعمل.

### F-09 · التخزين المحلي بلا حدود ولا معالجة خطأ
- **أين:** `lib/storage.ts` (LocalStore).
- **الخطأ:** localStorage يتسع ~5MB؛ المحادثات الطويلة تُفقد بصمت عند الامتلاء، و`saveAll` يتجاهل الأخطاء.
- **الحل (ساعة):** فحص الحجم قبل الحفظ + تنبيه "الذاكرة ممتلئة — صدّر محادثاتك". (ويُحل نهائيًا بمزامنة Neon من ROADMAP المرحلة 2.)

---

## 🟢 منخفضة — صقل لاحق

- **F-10 · وميض المظهر (FOUC):** الثيم يُطبق في `useEffect` بعد الرسم → وميض أبيض. الحل: سكربت inline في `<head>` يقرأ localStorage.
- **F-11 · الوصولية (a11y):** أزرار أيقونية بلا `aria-label`، وتركيز غير ظاهر. أضفها في مرحلة الواجهة.
- **F-12 · SEO/مشاركة:** لا OG tags — مشاركة الرابط تُظهر عنوانًا بلا وصف/صورة. سطران فقط.
- **F-13 · حجم الحزمة:** `rehype-highlight` يحمّل كل اللغات؛ قلّصها إلى الشائعة (js/ts/python/bash/json) — يكسب ~30KB.
- **F-14 · وضع العرض يستهلك دعوة serverless:** `demoStream` يبث من خادم؛ اجعله client-side (نص ثابت في الواجهة) لتوفير الدعوات المجانية — أولوية منخفضة لأنه رخيص.

---

## 🚀 خطة التنفيذ المقترحة (بلا هدم)

### الأسبوع 1 — "أغلق الثغرات" (يستحق قبل أي ميزة)
| اليوم | المهام | الساعات |
|---|---|---|
| 1 | F-01 (temperature) + F-06 (status Groq) + F-04 (تنبيه Demo) | 1.5 |
| 2 | F-03 (req.signal) + F-05 (CI يشغّل الاختبارات) | 1 |
| 3–4 | F-02 (Rate limit + حدود صارمة) | 4–6 |
| 5 | F-08 (اختبار HF الحقيقي وتنظيفه) | 2 |

### الأسبوع 2+ — عندها فقط الميزات الجديدة (بالترتيب)
1. **Neon sync** (يحل F-09 أيضًا) → 2. **Groq فعّال** → 3. **بحث ويب** → 4. **صور** → 5. **حسابات** (بعد الحماية — F-02 أولاً).

---

## ⛔ "لا تفعل" (ممنوعات مؤقتة)
1. **لا تفتح الـ API للعموم قبل F-02** — حصتك ستُستنزف.
2. **لا تدخل مفاتيح في الكود أو في أي ملف يُدفع** — تبقى env فقط.
3. **لا تربط حسابات/تسجيل دخول قبل الحماية** — دعوة مفتوحة للإساءة.
4. **لا تستبدل LocalStore قبل جاهزية Neon store** — ستفقد تاريخ المحادثات الحالي.
5. **لا ترفع النسخة إلى "مؤسسة" قبل** اختبارات E2E (مرحلة 6 في ROADMAP).

---

## 🎯 خلاصة النقد البنّاء
المستودع **أساسه ممتاز**: المعمارية تتحمل النمو (أثبتناها)، والتجربة والتنفيذ فوق المتوسط، والنشر متكامل. ما يعيقه الآن هو **البعد الأمني** (منطقي — كُتب للتشغيل المحلي ثم نُشر فجأة) و**مساران غير مكتملين** (temperature، حالة Groq). أغلق هذه النقاط الست خلال أسبوع، وسيصبح المشروع **جديرًا بجمهور حقيقي** — بدون إعادة كتابة أي سطر من المعمارية.

*المراجعة غير هدامة بالتصميم: كل الحلول أعلاه إضافات/استبدالات موضعية ضمن الحدود المحددة مسبقًا في ARCHITECTURE.md.*

---

## ✅ ملحق: نتيجة التنفيذ (2026-08-29)

نُفّذت الإصلاحات وتم التحقق منها — **كلها خضراء**:

| البند | الحالة | الدليل |
|---|---|---|
| F-01 · temperature يعمل | ✅ مُغلق | يمر من الواجهة → الخادم → كل المزودات + اختبار #12 |
| F-03 · إيقاف فعلي | ✅ مُغلق | `req.signal` + كسر الحلقة فورًا عند الإلغاء |
| F-04 · شفافية وضع العرض | ✅ مُغلق | بانر تنبيه في الواجهة عند `provider === "demo"` |
| F-05 · CI يشغّل الاختبارات | ✅ مُغلق | خطوة `npm test` بعد البناء في ci.yml |
| F-06 · حالة Groq | ✅ مُغلق | статус + الإعدادات + محدد الموديل |
| F-07 · إكمال الترجمة | ✅ مُغلق | 10 نصوص جديدة مترجمة (عربي/إنجليزي) |
| F-02 · حماية أساسية | 🟡 جزئي | حدود الحجم وmaxTokens والتحقق؛ Rate limit النهائي (Upstash) في المرحلة 7 |

**النتيجة:** `typecheck` ✅ · `build` ✅ · **12/12 اختبارات** ✅ · CI جديد يعمل بالاختبارات في الرفع القادم.
