# 🧪 تقرير اختبار نواة AI — الجولة الأولى

> **التاريخ:** 2026-08-29 · **البيئة:** نسخة الإنتاج (`next start`) مثل Vercel تمامًا
> **النتيجة النهائية:** ✅ **10 / 10 اختبارات ناجحة** · بناء سليم · صفر أخطاء في السجل

---

## 1) ملخص النتائج

| # | الاختبار | النتيجة | ملاحظات |
|---|----------|---------|---------|
| 1 | `GET /` — واجهة عربية RTL | ✅ | HTML سليم، `lang="ar"` + `dir="rtl"` |
| 2 | `GET /icon.svg` — أيقونة الموقع | ✅ | 200 OK |
| 3 | `GET /api/status` — حالة المفاتيح | ✅ | يعيد قيم منطقية بدون كشف المفاتيح |
| 4 | `GET /api/models` — قائمة الموديلات | ✅ | 7+ موديلات، كل المزودات حاضرة |
| 5 | `POST /api/chat` (demo) — البث الكامل | ✅ | 200 + `text/event-stream` + مقاطع كثيرة + `done` في النهاية |
| 6 | طلب Gemini بلا مفتاح → تراجع تلقائي | ✅ | يعمل فورًا، والحدث `provider: demo` |
| 7 | طلب HF بلا توكن → تراجع تلقائي | ✅ | نفس السلوك — **لا يفشل الطلب أبدًا** |
| 8 | رسائل فارغة → 400 | ✅ | تحقق صحيح من المدخلات |
| 9 | آخر رسالة ليست مستخدمًا → 400 | ✅ | تحقق صحيح من ترتيب الأدوار |
| 10 | سياق طويل (30 رسالة) → قص تلقائي | ✅ | نظام التشغيل لم ينهار، والقص التلقائي يعمل |

**البناء:** `next build` ✅ (0 أخطاء) · **الأنواع:** `tsc --noEmit` ✅ · **سجل الخادم:** نظيف، صفر استثناءات

---

## 2) ما الذي تم اختباره بالضبط؟

### أ) طبقة الخادم (API)
- ✅ بث SSE صحيح التنسيق: كل حدث `data: {...}` ويُغلق بـ `{done: true}`
- ✅ التراجع التلقائي بثلاث طبقات: `Gemini → HF → Demo`
- ✅ قص السياق تلقائيًا (آخر 12 رسالة) لمنع تجاوز حدود الحصة
- ✅ رفض المدخلات التالفة برسالة خطأ عربية واضحة

### ب) طبقة الذكاء (المزودات)
- ✅ `demo.ts` — يحاكي الكتابة الحية ببث تدريجي
- ✅ `gemini.ts` و `huggingface.ts` — الصياغة صحيحة (تُختبر فعليًا فور إضافة مفتاح)
- ✅ `resolveProvider()` — أيضًا لا يرمي أخطاء أبدًا

### ج) طبقة الواجهة (مؤكدة بالبناء والفحص الثابت)
- ✅ البناء ينتج JS/CSS سليمين (First Load 208KB — خفيفة لمثل هذا التطبيق)
- ✅ المكونات كلها تتزامن مع الأنواع (strict TS)

---

## 3) الاختبارات المعلّقة (بانتظار مفتاح حقيقي)

| الاختبار | أفضلية | طريقة التنفيذ |
|---|---|---|
| **Gemini فعلي** (2.5 Flash) | 🔴 عالية | ضع `GEMINI_API_KEY` ثم أعد `npm test` |
| **HF فعلي** (Mistral 7B عبر inference) | 🟡 متوسطة | ضع `HF_TOKEN` ثم أعد `npm test` |
| خطأ 429 (تجاوز الحصة) | 🟢 لاحقًا | محاكاة بإرجاع 429 نقدّر الرسالة |
| اختبار نقرة-بانقر على الواجهة | 🟢 لاحقًا | إضافة Playwright في المرحلة 6 |

**الخلاصة:** النواة **مستقرة وجاهزة** — الاختبارات تُشغَّل تلقائيًا بأمر واحد: `npm test` (وستعمل في GitHub Actions تلقائيًا بعد كل رفع — أضفنا `.github/workflows/ci.yml`).

---

## 4) بنية الاختبارات (كيف تشغّلها)

```bash
npm test                    # ضد الخادم المحلي (localhost:3000)
BASE_URL=http://localhost:3001 npm test   # ضد أي خادم آخر
```

- مكتوبة بـ **`node:test` المدمجة** — صفر اعتماديات إضافية
- ملف واحد: `tests/api.test.mjs`
- تعمل تلقائيًا في **GitHub Actions** (CI) بعد كل push إلى `main`

---

# 🧪 تقرير الاختبار — الجولة الثانية (جولة إصلاحات النقد البنّاء)

> **التاريخ:** 2026-08-29 · **النتيجة:** ✅ **12 / 12** اختبارًا ناجحًا

## ما أُضيف/أُصلح في هذه الجولة
| البند | الوصف | الحالة |
|---|---|---|
| F-01 | تمرير `temperature` فعليًا عبر الواجهة → الخادم → المزودات (كان ميتًا) | ✅ |
| F-03 | `req.signal` في `/api/chat` — الإيقاف يوقف التوليد فعلاً | ✅ |
| F-04 | بانر "وضع العرض التجريبي — أضف مفتاحًا مجانيًا" عند رد تجريبي | ✅ |
| F-05 | CI يشغّل **الاختبارات** بعد البناء (كان يبني فقط) | ✅ |
| F-06 | حالة Groq ممثلة في `/api/status` ولوحة الإعدادات ومحدد الموديل | ✅ |
| F-07 | استكمال الترجمة عربي/إنجليزي (نصوص لم تكن مترجمة) | ✅ |
| F-02 | حدود أولية: أقصى حجم للطلب (60KB) + `maxTokens` مقصوص (2048) + تحقق temperature | ✅ (Rate limit النهائي ينتظر Upstash — المرحلة 7) |

## الاختبارات الجديدة
- **#12** — إرسال `temperature` خارج النطاق (99) لا يكسر النظام ويُضبط بأمان
- تمت إعادة التحقق من الـ 11 اختبار السابقة كلها — كلها خضراء

## خطوات التحقق المطبقة
1. `npm run typecheck` → ✅ صفر أخطاء
2. `next build` → ✅ بلا أخطاء
3. `npm run start` + `npm test` → ✅ 12/12
4. سجل الخادم → نظيف بلا استثناءات

---

# 🧪 تقرير الاختبار — الجولة الثالثة (الاختبار الحي بالمفاتيح الحقيقية)

> **التاريخ:** 2026-08-29 · **البيئة:** النطاق الحي new-pro-kohl.vercel.app (مفاتيح حقيقية)

## 🏆 النتائج الحية على النطاق
| المزود | النتيجة | الزمن | الدليل |
|---|---|---|---|
| **Gemini 2.5 Flash** | ✅ يعمل | 787ms | `{"provider":"gemini"}` + رد «نواة تتكلم الآن.» |
| **Groq GPT-OSS 120B** | ✅ يعمل | 1048ms | `{"provider":"groq"}` + رد فوري |
| **Hugging Face** | ⚠️ يصل لكن يتطلب تفعيل حسابي | — | router يرد 400: «not supported by any provider you have enabled» |
| **المزامنة (Neon)** | ✅ مفعّلة | — | `{"enabled":true}` على النطاق الرئيسي |

## 🐛 خللان حقيقيان كشفهما الاختبار الحي (لم يكشفهما الاختبار المحلي)
1. **مرادف `hf:`** — المنسّق كان يقرأ `hf:` كمزود مجهول → يمر للتراجع التلقائي (يعمل محليًا بلا مفاتيح، لكنه يخطئ حيًا بمفاتيح). **أُصلح:** `hf` → `huggingface`.
2. **modèles Groq مؤرَّخة** — `llama-3.3-70b-versatile` لم يعد موجودًا على Groq. **أُصلح:** `openai/gpt-oss-120b` + `openai/gpt-oss-20b` (اختُبرا حيًا ✅).
3. **مكافأة:** `api-inference.huggingface.co` **متوقف عالميًا** (لا يستجيب DNS) — النطاق احتُجز/أُغلق. **أُصلح:** `router.huggingface.co` هو الأساسي.

## 📌 الدروس
- الاختبار المحلي (بدون مفاتيح) يغطي المسارات الآمنة لكنه **أعمى عن أخطاء التكامل الحقيقية** — هذا بالضبط سبب أهمية الاختبار الحي.
- النظام الآن: **مزودان يعملان فعلاً + تراجع تلقائي + مزامنة** — جاهز للإطلاق الفعلي.

---

# 🧪 تقرير الاختبار — الجولة الرابعة (حماية الحدود F-02)

> **التاريخ:** 2026-08-29 · **النتيجة:** ✅ **17 / 17** اختبارًا ناجحًا

## المنجز
- `lib/rate-limit.ts` — طبقة تقييد: نافذة 60 ثانية لكل IP (ذاكرة الخادم افتراضيًا) + ترقية تلقائية لـ Upstash عند إضافة المفاتيح
- `/api/chat`: حد 20 رسالة/دقيقة/IP → 429 + `Retry-After` + `X-RateLimit-*`
- `/api/conversations`: حد 60 طلب/دقيقة/IP
- `RATE_LIMIT_DISABLED=1` للاختبارات المحلية

## الاختبارات الجديدة
- **#16** — 25 طلبًا سريعًا: ضمن الحد ينجح (200)، وبعده يرفض (429) مع `code: RATE_LIMITED` وترويسة `Retry-After`
- **#17** — نقطة المزامنة تستجيب (حدها أعلى)

## ملاحظة مصداقية
الاختبار #16 يُشغَّل **أخيرًا** عمدًا لأنه يستهلك حصة الدقيقة — النظام كله (15/17 سابقًا) لا يتأثر بالحماية.

---

# 🧪 تقرير الاختبار — الجولة الخامسة (الحسابات Auth.js v5)

> **التاريخ:** 2026-08-29 · **النتيجة:** ✅ **21 / 21** اختبارًا محليًا + **برهان حي كامل**

## التغطية
- محلي: `GET /api/auth/status` → `enabled:false` (تدهور آمن) + `register` صالح → 503 + بريد غير صالح → 400 + كلمة مرور قصيرة → 400
- **حي (new-pro-kohl.vercel.app):**
  1. تسجيل `demo-...@nawah.app` → `{ok:true}`
  2. جلسة عبر CSRF → `{user:{id,email,name}}`
  3. دفع محادثة → `{account:true}`
  4. **جهاز ثانٍ بنفس الحساب → يرى "محادثة الحساب"** ✅ ← إثبات المزامنة عبر الأجهزة
  5. تنظيف + خروج

## ملاحظات
- `signout` عبر curl أعاد 405 (تفاصيل CSRF) — عميل المتصفح (`signOut()` من next-auth/react) يتعامل معها صحيحًا.

---

# 🧪 تقرير الاختبار — الجولة السادسة (لوحة المفاتيح BYOK)

> **التاريخ:** 2026-08-29 · **النتيجة:** ✅ **24 / 24** اختبارًا + إثبات حي

## المنجز
- `lib/keys.ts` — تخزين مفاتيح المتصفح (localStorage) + خريطة مزود→مفتاح
- `app/api/chat` — يقبل `apiKey` من اللوحة (يتقدم على البيئة بحد 300 حرف)
- `SettingsModal` — لوحة إدخال: 4 صفوف (Gemini/Groq/HF/Tavily) + إظهار/إخفاء + حفظ + حالة بيئة/محلي

## الاختبار الجديد
- **#18** — إرسال `apiKey` مع `search:web` → يبقى المزود `search` ولا يشتكي من غياب المفتاح (المسار يعمل)

## الإثبات الحي (new-pro-kohl)
- طلب بمفتاح وهمي من "اللوحة" → وصل Tavily فعلاً: `Unauthorized: missing or invalid API key` ← المسار كامل

---

# 🔎 نتائج تفعيل البحث الحي (Tavily)

> **التاريخ:** 2026-08-29 · المفتاح: `tvly-dev-...` (أُضيف Secret على Vercel → 201)

## الإثبات على new-pro-kohl.vercel.app
```
GET /api/status → {"gemini":true,"huggingface":true,"groq":true,"search":true}  ← 🔥 صحح
POST /api/chat modelId=search:web:
  provider: search
  "الخلاصة: Recent AI news includes significant developments in GPT models..."
  "### أحدث تطورات الذكاء الاصطناعي 2026 — دليلك الشامل"
  "...البيت الأبيض يجتمع مع OpenAI وجوجل وأنثروبيك..."
  (استمرت النتائج مع روابط المصادر حتى النهاية) — ~2.2 ثانية
```
- المفتاح يعمل من بيئة Vercel (أولوية) ومن لوحة المتصفح (BYOK) على حد سواء.

---

# 🧪 تقرير الاختبار — الجولة السابعة (تفعيل Google OAuth حيًا)

> **التاريخ:** 2026-08-29 · **النتيجة:** ✅ المزود مفعّل والسلسلة مُثبتة حيًا — بقي اختبار القبول النهائي من متصفح المستخدم

## الإثبات الحي (new-pro-kohl.vercel.app — نشر `dpl_6FLVNaggAtuD97fERRXgBwufAut7`)
| الخطوة | الدليل الفعلي |
|---|---|
| المفاتيح في Vercel | `AUTH_GOOGLE_ID` + `AUTH_GOOGLE_SECRET` → HTTP 201 (المفتاح لم يُخزَّن في أي ملف/مستودع) |
| المزود ظهر | `GET /api/auth/providers` → `"google": {"type":"oidc","callbackUrl":".../api/auth/callback/google"}` |
| الحالة | `GET /api/auth/status` → `providers: {google: true, github: false}` |
| التدفق الكامل | `POST /api/auth/signin/google` + CSRF → 302 إلى `accounts.google.com/o/oauth2/v2/auth?client_id=1082460812110-...&redirect_uri=.../api/auth/callback/google&scope=openid profile email` + PKCE |
| Google يقبلنا | طلب تفويض حقيقي بنفس client_id + redirect_uri → صفحة تسجيل دخول Google (لا `redirect_uri_mismatch`) |
| لا انكسار | `/api/status` ما زال `{gemini,huggingface,groq,search:true}` |

## المتبقي (اختبار قبول نهائي — يتطلب حساب Google في متصفح المستخدم)
زر «المتابعة عبر Google» → تسجيل الدخول → `/api/auth/callback/google` → جلسة → صف في `nahwa_users` → مزامنة عبر `user:<id>`.

---

# 🧪 تقرير الاختبار — الجولة الثامنة (AUTH IDENTITY HARDENING)

> **التاريخ:** 2026-08-29 · **النتيجة:** ✅ **37/37** (25 سابقة + 12 جديدة للهوية) · CI success

## المنجز في الشريحة (النطاق: auth/provisioning فقط)
- `lib/identity.ts` — `ensureApplicationUser()`: المصادقة OAuth → **canonical application user** (ذرّية + idempotent + بلا حالة جزئية) عبر `IdentityStore` (Port/Adapter).
- `lib/auth-db.ts` — إنشاء `nahwa_auth_identities` تلقائيًا (UNIQUE(provider, provider_account_id) + FK) + عمود `image`.
- `lib/auth.ts` — Provisioning **إلزامي داخل `callbacks.jwt`** قبل توقيع الجلسة (فشل = لا جلسة)؛ `session.user.id` = canonical.
- `app/api/conversations` — توثيق عقد النطاق: `user:<canonicalUserId>` فقط (لا معرّف مزود).
- `db/schema.sql` — المخطط الرسمي للجدولين.
- `tests/identity.test.ts` — 12 اختبارًا بهوية **اصطناعية** (لا حاجة لأي حساب بشري).

## الاختبارات الجديدة (Synthetic OAuth Harness — بلا Google حقيقي)
| # | العقد | النتيجة |
|---|---|---|
| 1 | هوية Google جديدة → مستخدم واحد | ✅ |
| 2 | نفس الهوية مرتين → نفس المستخدم | ✅ |
| 3 | نفس الهوية × 5 → مستخدم واحد | ✅ |
| 4 | هوية GitHub جديدة → مستخدم واحد | ✅ |
| 5 | ربط بريد موثق → نفس المستخدم (لا تكرار) | ✅ |
| 6 | فشل الربط → تراجع (لا حالة جزئية) | ✅ |
| 7 | الناتج دائمًا canonical nahwa_users.id | ✅ |
| 8 | النطاق = user:<canonical> فقط | ✅ |
| 9 | providerAccountId لا ينتج مكررين | ✅ |
| 10 | لا تخزين أي اعتماد مزود في سجل المستخدم | ✅ |
| 11 | Provisioning مستقل عن موافقة بشرية | ✅ |
| 12 | تكرار الدخول بعد الجلسة → نفس المستخدم | ✅ |

## الإثبات
```
typecheck ✅ · build ✅ · npm test = 37/37 ✅ (التوافق العكسي 25/25 سليم)
key-policy (BYOK) لم تُمس · لا أسرار جديدة · لا تغيير خارج نطاق auth/provisioning
```

---

# 🧪 تقرير الاختبار — الجولة الثامنة-ب (إغلاق سباق الهوية — Concurrency Safety)

> **التاريخ:** 2026-08-30 · **النتيجة:** ✅ **39/39** (25 API + 12 هوية + 2 سباق) · CI success

## لماذا هذه الجولة؟
اختبار السباق (`tests/identity-race.test.ts`) كشف أن `ensureApplicationUser` **لم يكن آمنًا تحت التوازي**:
قبل الإصلاح، 10 استدعاءات متزامنة لنفس هوية Google جديدة → **نجح 1 فقط**، والـ9 فشلت بـ
`duplicate email (UNIQUE nahwa_users.email)` — لأن كل الاستدعاءات تعبر SELECTين (لا شيء) قبل
أن يبدأ أي INSERT (نافذة check-then-insert غير الذرّية — مطابقة لسلوك Neon الحقيقي).
الأثر في الإنتاج: تبويبان/إعادة محاولة لتسجيل دخول واحد → فشل أحد الطلبات.

## الإصلاح المصغر (داخل نطاق الشريحة — lib/identity.ts فقط)
```
INSERT INTO nahwa_users (...) VALUES (...)
  ON CONFLICT (email) DO NOTHING
  RETURNING id
→ إن رجع صف: استخدمه        (الفائز)
→ إن لم يرجع: SELECT id WHERE email = ...  (converge على صف الفائز — سباق متوازٍ)
+ بريد صناعي حتمي عند غياب بريد المزود (sha256(provider:pid) → نفس الهوية → نفس البريد → converge)
+ linkIdentity يبقى ON CONFLICT DO NOTHING (آمن أصلًا)
```
**الفكرة:** UNIQUE(email) في قاعدة البيانات هو الحامي — لا الكود فقط (كما أوصى المراجِع).

## الأدلة
```
قبل:  RACE-R1 ❌ (1/10)  RACE-R2 ❌ (1/10)
بعد:  RACE-R1 ✅ (10/10 → صف واحد للمستخدم + صف واحد للهوية + نفس canonical)
       RACE-R2 ✅ (10/10 → نفس النتيجة تحت نافذة RTT غير ذرّية)
typecheck ✅ · build ✅ · npm test = 39/39 ✅ (التوافق العكسي سليم)
```

---

# 🧪 تقرير الاختبار — الجولة التاسعة (قراءة الملفات المرفقة — المرحلة 5.1)

> **التاريخ:** 2026-08-30 · **النتيجة:** ✅ **51/51** (25 API + 12 هوية + 2 سباق + 10 استخراج + 2 مرفقات API)

## المنجز
- `lib/file-extract.ts` — استخراج TXT/MD/CSV/JSON (مباشر) + PDF (pdf-parse v2.4.5، صنف `PDFParse`) + DOCX (mammoth)
- `POST /api/chat` — يقبل `files: {name, data(base64)}[]` (حد 3×1MB) ويُدمج النص في رسالة المستخدم الأخيرة؛ رفض صريح 400 للصيغ غير المدعومة/الحجم/JSON التالف
- الواجهة — زر 📎 + شريبات مرفقات قابلة للإزالة + i18n (AR/EN) + إرسال «ملف بلا نص»
- أمان — قائمة بيضاء بالامتدادات، حد حمولة 4.4MB (تحت Vercel 4.5)، لا حفظ على القرص، سقف نص الرسائل 60KB مستقل عن الملفات

## الاختبارات الجديدة
| # | الاختبار | النتيجة |
|---|---|---|
| EXT-1..6 | استخراج TXT/MD/CSV/JSON + **PDF حقيقي (pdfkit)** + **DOCX حقيقي (jszip)** | ✅ 6/6 |
| EXT-7..10 | رفض صيغة/حجم/JSON تالف/مرفق تالف + الدمج يلحق برسالة المستخدم | ✅ 4/4 |
| API #26 | مرفق TXT عبر /api/chat → 200 + بث كامل (provider + done) | ✅ |
| API #27 | مرفق .exe → 400 برسالة «غير مدعومة» | ✅ |

## الأدلة
```
typecheck ✅ · build ✅ · وحدات الاستخراج 10/10 (بمولّدات حقيقية) · npm test = 51/51 ✅
```

---

# 🧪 تقرير الاختبار — الجولة العاشرة (توليد الصور — المرحلة 5.2)

> **التاريخ:** 2026-08-30 · **النتيجة:** ✅ **63/63** (51 سابقة + 9 صور وحدة + 3 صور API)

## المنجز
- `lib/image.ts` — FLUX.1-schnell عبر HF: router أولًا ثم classic (تراجع تلقائي)، تعيين رموز HF لرسائل عربية، حدود (وصف ≤800 حرف، استجابة ≤5MB)، `imageKey()` BYOK>env
- `POST /api/image` — حد 10/دقيقة/IP (دلو مخصص) + تحقق prompt + 503 بلا مفتاح + 502 معقّم (sanitizeError)
- الواجهة — زر 🎨 يولد صورة من الوصف ويظهرها داخل المحادثة (Markdown image) + i18n AR/EN
- أمان — لا حفظ على القرص، سجل معقّم `[nawah][image-error]` (بلا مفتاح)، لا خدمة خارجية جديدة (HF مدمج أصلًا)

## الاختبارات الجديدة
| # | الاختبار | النتيجة |
|---|---|---|
| IMG-1..9 | router ينجح / تراجع classic / 401 / 403 تفعيل / وصف فارغ / وصف طويل / بلا مفتاح / استجابة غير صورة / BYOK | ✅ 9/9 |
| API #28..30 | بلا مفتاح → 503 · وصف فارغ → 400 · مفتاح وهمي → يصل HF (401) → 502 واضح | ✅ 3/3 |

## الأدلة
```
typecheck ✅ · build ✅ · npm test = 63/63 ✅ · CI بعد الرفع · الدليل الحي أدناه
```

# 🧪 تقرير الاختبار — الجولة الحادية عشرة (الصوت — المرحلة 5.3)

> **التاريخ:** 2026-08-30 · **النتيجة:** ✅ **72/72** (63 سابقة + 9 صوت)
> **النطاق:** Web Speech API — واجهة فقط، بلا خادم، بلا خدمة خارجية، بلا تكلفة.

## ما أُضيف
- `lib/speech.ts`: الغلاف الآمن لـ Web Speech API — إملاء (SpeechRecognition) + قراءة (speechSynthesis)،
  تنظيف Markdown قبل النطق (`cleanForSpeech`)، تقسيم النصوص الطويلة (`splitForSpeech`)،
  اختيار صوت عربي مفضّل (`pickVoice`)، اكتشاف الدعم (`speechAvailable`)، جلسات قابلة للحقن للاختبار.
- الواجهة (`app/page.tsx`): زر 🎤 إملاء في خانة الكتابة (يطلب إذن الميكروفون، مؤشر استماع نابض
  + «الاستماع… تحدث الآن»)، زر 🔊 «قراءة الرد بصوت» لحقل كل رد مساعد (يُظهر إيقاف القراءة أثناء التشغيل).
- i18n: مفاتيح AR/EN (dictation, listen, speechUnsupported, dictationError…).
- `tests/speech.test.ts`: 9 اختبارات (S-1..S-7 مع S-6b) على واجهات وهمية 100% — بلا شبكة ولا DOM.

## الأرقام
```
npm test   → 72/72 ✅  (30 API + 12 هوية + 2 سباق + 10 استخراج ملفات + 9 صور + 9 صوت)
typecheck  ✅ · next build ✅ · git grep أسرار نظيف ✅
CI (8c87717) → success ✅
```

## الدليل الحي (2026-08-30) — Chromium حقيقي عبر Playwright ضد النشر الحي
`https://new-pro-kohl.vercel.app` → **6/6 ✅**:
1. الصفحة تفتح وزر 🎤 الإملاء موجود («إملاء صوتي (Web Speech API)»)
2. دعم Web Speech API في المتصفح: `{"dictation":true,"tts":true}`
3. الضغط على 🎤 → **الإملاء بدأ فعليًا** (مؤشر الاستماع ظهر + رسالة «الاستماع… تحدث الآن»)
4. رسالة «قل مرحبًا بالعربية» → رد المساعد «مرحبًا!» (Gemini 2.5 Flash)
5. زر 🔊 «قراءة الرد بصوت» يظهر على رد المساعد ويستدعي `speechSynthesis` بلا أخطاء
6. لا أخطاء JavaScript قاتلة

> لقطة الشاشة الكاملة: `/home/user/artifacts-5-3.png` (زر الميكروفون نابض + «الاستماع» + أزرار
> نسخ/قراءة الرد بصوت/إعادة التوليد على رد المساعد).

## الحالة
**5.3 = PASS** (اختبارات + CI + دليل حي في متصفح حقيقي). تُغلق الشريحة.

# 🧪 تقرير الاختبار — الجولة الثانية عشرة (التذكّر — المرحلة 5.4)

> **التاريخ:** 2026-08-30 · **النتيجة:** ✅ **85/85** (72 سابقة + 8 تذكّر + 5 مزودات)
> **النطاق:** ملخص ذكي تلقائي للمحادثات الطويلة — استخلاصي محلي 100% (بلا مزود، بلا تكلفة، بلا خدمات خارجية).

## ما أُضيف
- `lib/summary.ts`: `shouldSummarize` (عتبة 24 رسالة) · `summarize` (ملخص عربي من أسئلة/أجوبة،
  نافذة حية 12 رسالة، حد 3500 حرف) · `composeSystem` (دمج النظام + الملخص في system واحد).
- `lib/types.ts`: حقل `Conversation.summary` (يُزامن تلقائيًا عبر Neon — المرحلة 2).
- الواجهة (`page.tsx`): عند الإرسال، إن تجاوزت المحادثة العتبة يُبنى الملخص ويُمرَّر مع `system`
  + بطاقة 🧠 «التذكّر» قابلة للطي أعلى الرسائل.
- **إصلاح خلل كامن:** `system` كان يُقرأ في `/api/chat` لكنه لم يمر إلى `streamReply` إطلاقًا —
  الآن يمر فعليًا إلى Gemini (`systemInstruction`) وGroq/HF (رسالة system أولى) — شرط عمل 5.4.

## الأرقام
```
npm test   → 85/85 ✅  (30 API + 12 هوية + 2 سباق + 10 استخراج + 9 صور + 9 صوت + 8 تذكّر + 5 مزودات)
tests/summary.test.ts       8/8  (R-1..R-8)  — وحدة مباشرة بلا شبكة
tests/provider-system.test.ts 5/5 (PS-1..PS-5) — mock fetch يثبت وصول system للمزودات الثلاثة
typecheck ✅ · next build ✅ · git grep أسرار نظيف ✅ · CI (4c42d0e= success)
```

## الدليل الحي (2026-08-30) — مسار حقيقي عبر Chromium/Playwright على النشر الحي
محادثة حقيقية بوضع العرض التجريبي (بلا حصة) — 28 رسالة ثم رسالة تفعيل → **9/9 ✅**:
1. **A Trigger:** تجاوزت المحادثة العتبة (n=28 > 24) → الملخص وُلد (1513 حرفًا)
2. **A Generation:** يذكر الأسئلة القديمة (السؤال 1) ولا يضم الرسائل الحية (السؤال 12+)
3. **B Persistence:** الملخص محفوظ في `Conversation.summary` (مع `nawah:convs` — يُزامن)
4. **C Prompt propagation:** آخر `POST /api/chat` يحمل `system` = 1579 حرفًا يتضمن
   «ملخص المحادثة السابقة» + «السؤال 1» (اعتراض مباشر للجسم الفعلي المرسل من الواجهة)
5. **واجهة:** بطاقة 🧠 «التذكّر — ملخص تلقائي» ظاهرة · لا أخطاء JS
6. **Provider propagation:** مثبتة بوحدات بلا شبكة (PS-1..5): Gemini → `systemInstruction`،
   Groq/HF → رسالة system أولى، وبلا system لا كسر للمسار القديم.
   (ملاحظة بيئية: حصة Gemini المجانية كانت 429 أثناء الاختبار الحي — لم تُحرق؛
   المسار الكامل gemini + system مثبت بالوحدة PS-1 وبطلب حي سابق 200 على `/api/chat`.)

> **اختبار سابق (حُوكم):** محاولة حقن محادثة في localStorage فشلت في العرض (الـ Sidebar يقرأ
> من الحالة لا من التخزين مباشرة) — صُنّفت **TEST HARNESS ISSUE**، لا عطب منتج؛
> استُبدلت بالمسار الحقيقي أعلاه.

## الحالة
**5.4 = PASS** — acceptance كامل: Trigger/Génération/Persistence/System/Providers + Regression
(Gemini/Groq/HF بقيم system المزوّدة) + typecheck/build/85/85/CI + Live Verification + Evidence.
