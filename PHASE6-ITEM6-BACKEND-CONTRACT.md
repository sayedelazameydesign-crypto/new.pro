# CR-006 (المرحلة: CONTRACT) — Backend Contract — Conversation Intelligence

> **حالة هذه الوثيقة: AWAITING OWNER APPROVAL (Contract only — لا كود، لا UI).**
> المرجع المعياري: `FRONTEND-SPEC.md` §10 (Item 6) — «لا واجهة بلا عقد».
> مخرَج هادم لاعتماد قرارات المالك (§9) — بعدها يُشرَع التنفيذ في CR منفصلين (خادم ثم واجهة) يُكمِل كل منهما Evidence.

## 1) تعريف

| الحقل | القيمة |
|---|---|
| CR-ID | `CR-006` (هذه الوثيقة = مرحلة Contract بالتحديد) |
| العنوان | Conversation Intelligence — عنوان أفضل للمحادثة + (لاحقًا) إكمال |
| الحالة | **Contract — Awaiting Approval** (لا تنفيذ قبل توقيع §9) |
| Reference Commit | `c02fdb2` (بعد PR #5؛ المستند البرمجي للمشروع = `8918512` بلا تغيير كود) |
| الفرع المقترح للتنفيذ (لاحقًا) | `cr/item-6-intel` |

## 2) الهدف (قابل للتحقق)

1. توليد **عنوان أفضل** للمحادثة عبر مزود AI (بدل قص 34 حرف + … في `titleFromMessages`)، يُحفظ في `Conversation.title` **الموجود أصلًا** (لا تغيير مخطط).
2. بلا أي تغيير على `/api/chat` (مسار البث مقدّس — منعًا للتعارض مع E2E القائم).
3. بلا مزود جديد، بلا بيئة جديدة، بلا schema DB جديدة، بلا تخزين خادمي للمحتوى.
4. فشل آمن: لا اقتراح خاطئ/فارغ — وصفة `null` → العميل يستمر بالعنوان المحلي القديم.

## 3) الحقائق المعمارية (مفحوصة من الكود — أساس العقد)

| الحقيقة | الدليل | أثرها على العقد |
|---|---|---|
| الخادم **stateless** بلا قراءة محادثات من قاعدة بيانات داخل `/api/*` (المحتوى يُرسَل في الطلب — نمط `/api/chat` يستقبل `messages`) | `app/api/chat/route.ts` | الطلب يحمل **المصدر المُقلَّص** — لا `conversationId` كمرجع خادمي، ولا DB lookup |
| `summary.ts` (**5.4**) = **استخلاص محلي خالص** — لا استدعاء مزود | `lib/summary.ts` (بناء نص محلي عبر keySentence/clip) | **لا سابقة** لإرسال مولّد خارج مسار الرد → قرار خصوصية جديد (§7) |
| المزود/التراجع الحقيقي: **المطلوب → Groq → Gemini → HuggingFace → demo**؛ `search` مستقل بلا تراجع | `lib/ai/index.ts` `resolveProvider` | يُعاد استخدامه كما هو **حرفيًا** |
| `streamReply(opts)` = async generator (chunks) — يُجمَّع كامل النص ثم يُرجع JSON (لا حاجة لـSSE هنا) | `lib/ai/index.ts` | عقد الـintel = **JSON بسيط** (لا بث)، جمع بحد `maxTokens ≤ 80` |
| `checkRateLimit(bucket, ip, limit?)` — buckets موجودة (chat/sync/auth) | `lib/rate-limit.ts` | bucket جديد `"intel"` |
| `sanitizeError(err, ...secrets)` موجود | `lib/ai/index.ts` | يُلزم في عقد الأخطاء — لا تسريب مفاتيح/محتوى |
| `Conversation.title` حقل موجود (string) + `titleFromMessages` fallback | `lib/types.ts` / `app/page.tsx` | **لا migration**؛ الفولي باك المحلي يبقى |
| زر/mode المستخدم له مزود مفعّل في `settings.modelId` | `lib/models.ts` / `app/page.tsx` | قرار ما يُرسَل ولمن (§7) |

## 4) النطاق / خارج النطاق

**داخل:</b> endpoint جديد واحد + استخدام `lib/ai` القائم + صفر تغيير على `/api/chat`/`/api/conversations`/`db/*`/`validation.ts` الحالية (تُضاف schema **جديدة** في ملف منفصل أو قسم intel منفصل — قرار تنفيذ لاحق).
**خارج (قرارات معلنة):**
- ❌ `kind: "completion"` عبر AI — **محلي أولًا** (اقتراح إكمال من سياق العميل؛ لا طلب لكل توقف كتابة — تأخّر/كلفة/خصوصية). العقد يدعم توسيع الـkind لاحقًا بنفس الـendpoint فقط بقرار مستقل.
- ❌ أي مشاركة عامة/روابط (Local Share يبقى كما هو — خارج Item 6).
- ❌ تغيير `lib/summary.ts` أو منطق التذكّر.
- ❌ تخزين خادمي للعناوين/سجل بيانات المستخدم (النتيجة تعود للعميل وتُحفظ عبر المسار القائم فقط: `Conversation.title` → autosave/Neon).
- ❌ أي dependency جديدة (لا حزم تلخيص/عنوان).

## 5) العقد (Contract)

### 5.1 Endpoint
**`POST /api/conversation-intel`** (جديد) — القرار الموصى به.
- البديل المرفوض: «إشارة داخل `/api/chat`» — يلوث مسار البث + يصعّب حدود مستقلة + يعقد عقود/اختبارات قائمة (ويتعارض مع «path مقدّس» أعلاه). لا يُعتمد إلا بطلب صريح.

### 5.2 Request (Zod v4 — نفس نمط `chatRequestSchema`)
```ts
export const intelRequestSchema = z.object({
  kind: z.enum(["title"]),                       // extension: ["title","completion"] لاحقًا فقط بقرار
  source: z.string().min(1).max(2000),           // مقلَّص بالعميل: أول رسالة مستخدم (انظر §7)
  lang: z.enum(["ar", "en"]).default("ar"),      // يضبط لغة المخرَج
});
```
- لا `conversationId` (لا مرجع خادمي — معلل §3) · لا `apiKey` من العميل هنا؟ **قرار §9-د**: إتاحة BYOK لنفس لوحة المفاتيح (يتماشى مع سياسة BYOK القائمة ولا يكشف مفتاحًا في سجل الخادم — يُمرَّر في الجسم مثل /api/chat). **التوصية: نعم** (تطابق سلوك /api/chat: `apiKey: z.string().max(300).optional()`).

### 5.3 Response
```ts
// نجاح (200)
{ ok: true, value: string /* ≤80 */, provider: "groq"|"gemini"|"huggingface", model: string, generatedAt: number }
// فشل آمن (200 مع ok:false — لا نخطئ برمجيًا، أو 429/400؛ لا 500 يكشف تفاصيل)
{ ok: false, code: "invalid" | "no-provider" | "provider-error" | "rate-limited", error: string /* مُعقَّم عبر sanitizeError — بلا مفاتيح/محتوى */ }
```
- **لا `demo` في provider**: غياب كل المفاتيح → `ok:false, code:"no-provider"` **بلا استدعاء إطلاقًا** (لا نخيّل عنوانًا وهميًا؛ العميل ينزل لـ`titleFromMessages`). (قرار §9-ب).

### 5.4 Provider resolution
- `resolveProvider(settings.modelId)` كما هو — بنفس سلسلة التراجع الحقيقية (المطلوب → Groq → Gemini → HF). لا ترتيب مخصص.
- `maxTokens: 40–80` (عنوان قصير: يقلل كلفة/تشويش) + `temperature: ≤0.3` (استقرار) — قيم افتراضية مقترحة محددة دقيقًا في العقد، قابلة للـCR عند التنفيذ.

### 5.5 Fallback على مستوى العقد
- فشل مزود/مهلة/429 → `ok:false` **بلا كتلة فارغة**؛ العميل يُبقي `Conversation.title` الحالي (أو `titleFromMessages` للمحادثة الجديدة) — **لا حالة «عنوان مكسور»**.
- no-op كامل عند أول رسالة؟ لا: يُولَّد عند أول رسالة مستخدم (بعدها يكون هناك مصدر). إن فشل → يُعاد عند الإرسال التالي (دون حرق الحدود — debounce).

### 5.6 Rate limit (قرار §9-هـ)
- bucket جديد `checkRateLimit("intel", ip, 10)` — توصية: **10/دقيقة/IP** (نصف chat: العنوان ليس حساسًا زمنيًا).
- + حد عميل: **طلب واحد لكل محادثة كل 30 ثانية** (debounce) — لا تكرار على autosave.
- `RATE_LIMIT_DISABLED=1` يشمله (بيئة الاختبار فقط — نفس قاعدة القائمة).

## 6) الأمان والعقود

- لا أسرار جديدة (لا env جديدة؛ مزودات موجودة فقط)؛ `sanitizeError` إلزامي على كل رسالة خطأ.
- لا تسجيل لمحتوى المستخدم: سجل الخادم بالسبب فقط (نمط `[nawah][provider-fallback]`).
- لا أثر على صلاحيات/جلسات (`/api/conversations` + auth) — الـintel endpoint عام مثل `/api/chat` (بلا بيانات مخزّنة).
- `source ≤ 2000` + قص عميل إجباري (مطابق لسقف 30,000 للملفات — أصغر هنا لأن الهدف عنوان).
- git grep قبل/بعد أي تنفيذ (معيار جاري).

## 7) Privacy Boundary — **مصفوفة القرار (الجوهر)**

> السياق: حاليًا **لا** يُرسل أي محتوى خارج مسار رد `/api/chat` (`summary.ts` محلي). توليد العنوان = **أول استخدام للبيانات خارج هذا المسار** → قرار صريح.

| الخيار | ماذا يُرسل | لمن | الكلفة/الخصوصية | التوصية |
|---|---|---|---|---|
| **A — تحيّز الأدنى (موصى)** | **أول رسالة مستخدم فقط** (بعد التنظيف: بلا تنسيق مبالغ، ≤2000 حرف) | **نفس مزود conversation** (سلسلة التراجع الحقيقية — نفس أي بيانات أرسلها المستخدم أصلًا للرد) | أقصى خصوصية ممكنة مع «ذكاء» حقيقي؛ لا محادثة كاملة، لا تعليمات نظام، لا مرفقات | ✅ |
| B — رسالتان (مستخدم+رد أول) | سياق أوسع | نفس المزود | خصوصية أقل قليلًا | ✖ (لا فائدة تذكر للعنوان) |
| C — محلي فقط (صفر إرسال) | لا شيء | لا أحد | مثالي خصوصيًا لكن «العنوان الذكي» لا يتحقق (يبقى 34 حرفًا) | ✖ (ضد هدف Item 6) |
| D — محادثة كاملة | كل الرسائل | مزود خارجي | **مرفوض** (امتداد بلا داعٍ) | ❌ |

**حقل إعدادات (اختياري، متوافق للخلف):** `smartTitle?: boolean` في `Settings` — إن اعتُمد **A** فالتوصية: **افتراضي ON + مفتاح إيقاف في الإعدادات** (لأن المستخدم وافق أصلًا على إرسال رسائله لنفس المزود؛ لا التزام اختياري عكسي). البديل المحافظ: opt-in صريح. (قرار §9-ج).

## 8) الاختبارات المخطط لها (تُنفَّذ بعد اعتماد العقد — لا الآن)

- **وحدة (node:test):** schema (قبول/رفض/حدود)، قص `source`، تراجع `resolveProvider` بلا مفتاح → `no-provider` (لا استدعاء)، sanitize أخطاء، حدود debounce (pure).
- **E2E (Playwright — مع UI لاحقًا):** إرسال رسالة → عنوان من المزود يظهر في sidebar (أو `titleFromMessages` عند محاكاة فشل)؛ لا كسر لـE2E-7/تسمية يدوية.
- أرقام مرجعية: 163(+) وحدة · 19(+) E2E — بلا تعديل على أي اختبار قائم.

## 9) قرارات المالك المطلوبة (✅/تعديل — بدونها لا تنفيذ)

| # | القرار | القرار المعتمد (المالك — 2026-08-30) |
|---|---|---|
| أ | Endpoint جديد `POST /api/conversation-intel` (مستقل) | ✅ **معتمد** |
| ب | لا استدعاء عند غياب المفاتيح (`no-provider` → العميل يتولى) — **بلا مشاركة demo** | ✅ **معتمد** |
| ج | خصوصية: **A (أول رسالة فقط، نفس المزود)** + `smartTitle` افتراضي ON قابل للإيقاف | ✅ **معتمد** |
| د | BYOK داخل الـintel (نفس لوحة المفاتيح — تطابق /api/chat) | ✅ **معتمد** |
| هـ | bucket `"intel"` 10/دقيقة/IP + debounce 30ث/محادثة | ✅ **معتمد** |
| و | `completion` = **محلي أولًا** (خارج عقد الخادم حتى قرار مستقل) | ✅ **معتمد** |

> **توقيع الاعتماد:** اتُّخذت القرارات أ–و جميعًا كما هي (بدون تعديل). العقد الآن **Baseline رسمي لتنفيذ Item 6**. التاريخ: 2026-08-30.

## 10) DoD (عند تنفيذ العقد لاحقًا)

- [ ] توقيع §9 (كل القرارات).
- [ ] وحدة + E2E كاملة (اختبارات القائمة 163/19 بلا تعديل).
- [ ] `lint 0/0` · typecheck · build ✓ · Security grep نظيف · Contract review (لا تغيير chat/conversations/auth/db/validation القائمة).
- [ ] Dependency Review: **صفر جديد** (لا env جديدة).
- [ ] تحديث FRONTEND-SPEC §10/§06 (Item 6 → حالة جديدة بEvidence) + DEVELOPMENT-STATE **في نفس PR** (كما قرر المالك: تحديث واحد للمرجعين).
- [ ] بعد الدمج: PR مراجعة بشرية ثم smoke حي (chunk يحمل علامات Item 6) — وبعدها فقط PASS في §06.

## 11) المراجع
- `FRONTEND-SPEC.md` §10 (Item 6) · §13 (Evidence Index) — المعيار.
- كود مرجعي: `lib/ai/index.ts` (resolveProvider/streamReply/sanitizeError) · `lib/rate-limit.ts` (checkRateLimit) · `lib/validation.ts` (نمط zod v4) · `lib/types.ts` (`Conversation.title`) · `lib/summary.ts` (محلي — أساس قرار الخصوصية) · `app/api/chat/route.ts` (نمط الاستقبال/الأمان).
- سوابق: PHASE6-ITEM2/3/4/5-AUDIT.md (قرارات محلية/no-new-dep).
