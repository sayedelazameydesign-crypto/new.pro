# Phase 6 / Item 2 — Audit (قبل التعديل)

> سُجِّل قبل أي تعديل — وفق أمر التنفيذ (قسم 1).

## 1) نقطة دخول المحادثة
- `app/page.tsx` — `active = conversations.find(c => c.id === activeId)` (سطر ~66).
- العرض: منطقة «الرسائل» `flex-1 overflow-y-auto` (سطر ~701) تعرض `active.messages.map(...)`، يليها اختيار الموديل + صندوق الإدخال. الرأس العلوي (سطر ~596) فوق الكل.

## 2) شكل بيانات الرسائل (`lib/types.ts`)
- `Conversation { id, title, createdAt, updatedAt, messages: ChatMessage[], summary? }`
- `ChatMessage { id, role: "user" | "assistant", content, model?, createdAt }`
- **لا مفاتيح ولا أسرار داخل shape** — التصدير لا يمس `Settings` (الذي يحوي `apiKey` في المتصفح) إطلاقًا.

## 3) أفضل مكان لـ Read Mode
- نفس منطقة الرسائل: عند تفعيل Read Mode يُعرض مكوّن جديد `app/components/ReadMode.tsx` **بدل** فقاعة المحادثة داخل نفس حاوية التمرير (بلا إعادة بناء، بلا تغيير سلوك الإرسال).
- زر الدخول في الرأس العلوي بجوار الإعدادات (ظاهر عند وجود محادثة نشطة)؛ الخروج بزر «عودة» داخل Read Mode + تلقائيًا عند تبديل المحادثة.
- **بلا أي طلب شبكة** — حالة محلية فقط.

## 4) أفضل مكان لتصدير Markdown
- `lib/export.ts` جديد (pure، قابل للاختبار node): بناء Markdown + اسم ملف sanitized + تنزيل Blob.
- زر في رأس Read Mode: «تصدير Markdown» → تنزيل `.md` محليًا (بلا خادم، بلا شبكة).

## 5) أفضل طريقة لـ PDF ضمن الـstack الحالي
- **طباعة المتصفح: `window.print()` + `@media print` في globals.css** — «حفظ كـ PDF» من حوار الطباعة.
- الأسباب: دعم عربي/RTL كامل عبر محرك نص المتصفح (أعلى جودة بلا خطوط مضمّنة)، بلا dependency، بلا تغيير Chat runtime، يعمل Offline.
- القيد الموثق: المستخدم يختار «حفظ كـ PDF» من الحوار (لا يُنشأ ملف .pdf برمجيًا بلا مكتبة).

## 6) هل يمكن PDF بدون dependency جديدة؟
- **نعم — print/PDF المتصفح كافٍ.**
- بديل مُحصَّن ومراجع: `pdfkit` موجود أصلًا في dependencies لكنه **DEV ONLY** (توليد عينات في الاختبارات فقط)؛ استخدامه للإنتاج يتطلب تضمين خط عربي + معالجة تشكيل (shaping) — خطر حجم/جودة، **مرفوض** لهذا البند.
- `jsPDF` = dependency جديدة بخط عربي مضمّن — مرفوض (نفس السبب).

## قرارات التصميم (الأقل تغييرًا، الأقل dependencies)
| القرار | البديل المرفوض |
|---|---|
| Read Mode = مكوّن واحد فوق نفس منطقة الرسائل | إعادة بناء منطقة المحادثة |
| Markdown = `lib/export.ts` pure + Blob | POST إلى خادم (شبكة خارجية ممنوعة) |
| PDF = `window.print()` + CSS | مكتبة PDF جديدة / pdfkit إنتاجي |
| لا dependency جديدة إطلاقًا | — |
