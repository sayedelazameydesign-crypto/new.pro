# 🛣️ خطة التطوير والتحديث الكاملة — نواة AI

> **الحالة:** النواة v0.1 جاهزة (مختبرة ✅ 10/10) — هذه الخطة تنتقل بها من "نواة تعمل" إلى **"منصة كاملة"**.
> **المبدأ:** كل مرحلة تُبنى فوق السابقة **دون هدم** — استبدال تنفيذ، لا إعادة كتابة.

---

## 🎯 الرؤية
نواة AI: **مساعد ذكاء اصطناعي مجاني 100%** لكل العرب — بلا بطاقة، بلا تعقيد، قابل للتوسع إلى منصة كاملة (محادثات، ملفات، بحث، صور، صوت، حسابات، مزامنة).

## ⚖️ معايير الأولوية (تُطبَّق على كل مرحلة)
1. **مجاني بالكامل** — لا أي خدمة تطلب بطاقة
2. **لا هدم** — أي ميزة عبر إضافة ملف/معرّف جديد
3. **تأثير فوري** — يراها المستخدم بنفس اليوم
4. **قابل للإلغاء** — كل مرحلة مستقلة، يمكن تخطيها دون كسر الباقي

---

## 🔴 المرحلة 1 — الإطلاق الحقيقي (أسبوع واحد) — *الأهم الآن*
**الهدف:** التطبيق يرد من Gemini فعليًا على رابط Vercel عمومي.

| المهمة | التفاصيل | الملفات المتأثرة |
|---|---|---|
| 1.1 | أنشئ مفتاح Gemini من [AI Studio](https://aistudio.google.com/apikey) (مجاني، 30 ثانية) | — |
| 1.2 | ارفع مستودع GitHub (مكتمل ✅ → `new.pro`) | — |
| 1.3 | استورد في Vercel → Deploy → أضف `GEMINI_API_KEY` → Redeploy | — |
| 1.4 | فعّل CI تلقائيًا: **Actions → Enable** (مكتمل ✅ `ci.yml`) | — |
| 1.5 | جرّب موديلات Gemüni الثلاثة + HF وإعداد System Prompt | — |

**تعريف الإنجاز:** رسالة حقيقية من Gemini على الرابط العمومي خلال <10 ثوانٍ.

## 🟠 المرحلة 2 — الذاكرة والمزامنة السحابية ✅ مكتملة
**الهدف:** محادثاتك تتبعك على أي جهاز.

> ✅ **اكتملت وتعمل على الموقع الحي** (2026-08-29): `lib/storage-neon.ts` + `/api/conversations` (GET/PUT/DELETE) + دمج ذكي في الواجهة + مؤشر ☁️ + اختبارات (15/15).
> ✅ `DATABASE_URL` مُفعّل على Vercel (Secret) + الجدول أُنشئ تلقائيًا + دورة حية مثبتة (دفع/سحب/تحديث/حذف).
- نفّذ واجهة `ConversationStore` بـ **Neon (Postgres مجاني)** — `lib/storage/neon.ts`
- إضافة جدول `conversations` + `messages` (JSONB) — مفتاح `device_id` في `localStorage`
- `GET /api/conversations` + `PUT /api/conversations` (Route Handlers جديدة — بدون لمس الواجهة)
- احتفظ بـ LocalStore كوضع "بدون حساب"

**تعريف الإنجاز:** أرسل من الهاتف، اقرأ من الحاسوب — بنفس الحساب الجهازي.

## 🟡 المرحلة 3 — الحسابات (اختياري، 5–7 أيام)
**الهدف:** تسجيل دخول GitHub/Google عبر **Auth.js v5** (مجاني 100%).
- `/api/auth/[...nextauth]` + صفحة تسجيل بسيطة
- ربط `userId` بجداول المحادثات
- جدار حماية: `middleware.ts` لحماية المسارات (اختياري)

> 💡 نصيحة: **لا تؤجل الإطلاق للمرحلة 3** — الإطلاق بلا تسجيل أدخل وأسرع، والتسجيل يُضاف لاحقًا دون هدم.

## 🟢 المرحلة 4 — مزودات ذكاء إضافية (2–3 أيام)
**الهدف:** تنويع + تجاوز حدود الحصة المجانية تلقائيًا.
- **Groq** (مجاني وسخي جدًا: Llama 3.3 70B) — `GROQ_API_KEY`
- **OpenRouter** (نموذج `:free` — بلا حدود يومية معقولة)
- **DeepSeek** (سعر رخيص، لكن يبدأ مجانًا بحدود)
- توسيع `resolveProvider()` إلى قائمة أولويات: `Groq → Gemini → HF → Demo`
- كل ذلك عبر نفس قواعد الإضافة: ملف مزود + سطر case + سطر في `models.ts`

**تعريف الإنجاز:** عند انتهاء حصة Gemini ينتقل التطبيق تلقائيًا لـ Groq دون أن يشعر المستخدم.

## 🔵 المرحلة 5 — قدرات متقدمة (1–2 أسبوع)
| القدرة | الخدمة المجانية | التنفيذ |
|---|---|---|
| **🌐 بحث ويب** | SearXNG (مفتوح) أو Tavily/Serper 1000 طلب/شهر | أداة `web_search` في `lib/ai/v2.ts` |
| **🖼️ توليد صور** | FLUX.1-schnell عبر HF (مجاني) | `POST /api/image` + عرض في المحادثة |
| **📄 قراءة ملفات** | `pdf-parse` + `mammoth` (مفتوح) | رفع في `/api/chat` → تضمين نص الملف |
| **🎤 صوت** | Web Speech API (المتصفح — بلا خادم) | إملاء + قراءة الردود بصوت عربي |
| **🧠 تذكّر** | الجلسة + قواعد البيانات من المرحلة 2 | ملخص ذكي تلقائي للمحادثات الطويلة |

## 🟣 المرحلة 6 — واجهة وصقل (1 أسبوع)
- **PWA**: قابل للتثبيت على الهاتف (manifest + service worker)
- **وضع القراءة**: تصدير محادثة Markdown / PDF
- **مشاركة رابط**: `?c=id` يفتح محادثة عامة (اقرأ فقط)
- **اختبارات E2E**: Playwright (3 اختبارات: إرسال، حذف، تغيير إعدادات)
- **تحسينات**: اختصارات لوحة مفاتيح، سحب الملفات للإدخال، وضع ملء الشاشة
- **ذكاء**: اقتراح تلقائي لاستكمال الرسالة + عنوان أفضل للمحادثة

## ⚫ المرحلة 7 — أمان واستقرار (2–3 أيام)
- **تقييد**: `middleware.ts` + Upstash Redis (مجاني) — حد 20 طلب/دقيقة/جهاز
- **حماية API**: رفض الطلبات الكبيرة (`content-length`)، فحص `role`
- **خصوصية**: إعداد "حذف كل البيانات" + موافقة صريحة
- **مراقبة**: Vercel Analytics (مجاني) + تنبيهات أخطاء

## 🏁 المرحلة 8 — ما بعد الحصص + المجتمع (مستمر)
- صفحة **Landing beautiful** + لوحة تحكم إحصاءات
- **Dockerfile** + نشر ذاتي (Render Railway مجاني)
- وثائق عربية كاملة + مدونة "كيف بنيت نواة"
- ترجمة كاملة للإنجليزية + دعم أكثر (فرنسي/إسباني عبر i18n)

---

## 📅 الجدول الزمني المقترح (6 أسابيع)

| الأسبوع | المرحلة | الإنجاز |
|---|---|---|
| 1 | 1 + 2 | إطلاق حقيقي + مزامنة سحابية |
| 2 | 4 + 7 | مزودات متعددة + أمان |
| 3 | 5 (بحث + صور) | قدرات متقدمة |
| 4 | 5 (ملفات + صوت) | تجربة غنية |
| 5 | 3 + 6 | حسابات + واجهة مصقولة |
| 6 | 6 + 8 | PWA + إطلاق مجتمعي |

---

## 📊 فوائد متوقعة بعد التنفيذ
- **تجربة خالية من الانقطاع:** 4 مزودات بتراجع تلقائي = حصة شبه لامنتهية مجانًا
- **وصول من أي جهاز:** مزامنة فورية عبر Neon
- **مميزات غير موجودة حتى في التطبيقات المدفوعة:** بحث ويب + صور مجانية
- **أمان صناعي:** حدود طلبات + حماية من الإساءة قبل فتح الرابط للعموم

---

## ✅ جاهز للانطلاق؟
المرحلة التالية المقترحة (يمكنني تنفيذها الآن مباشرة):
1. **إضافة مزود Groq المجاني** (ملف واحد + سطرين) — يعمل بمجرد إضافة المفتاح
2. **Neon sync** — أبدأ بها فور إعطائي رابط قاعدة البيانات

> 📖 *التفاصيل الهندسية لكل مرحلة في `ARCHITECTURE.md` — الملفات والواجهات محددة مسبقًا.*
